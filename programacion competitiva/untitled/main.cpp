#include <bits/stdc++.h>
using namespace std;

int main(){
    long long int test=0;
    long long int n=0;
    long long  int k=0;
    cin>>test;
    //profe si lee esto, vale pseudocodigo? :V
    // saco log(n) en base 2
    // luego con ese valor obtenido hago combinatorias de i hasta el valor y las sumo
    // finalmente le sumo 1 por si no compra nada
    //pipipi falto tiempo xd
    cout<<0;
    return 0;
}

